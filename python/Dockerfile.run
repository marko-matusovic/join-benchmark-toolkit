# mmatusovic/python:run IMAGE

FROM mmatusovic/python:dev

WORKDIR /app/join-benchmark
COPY join-benchmark .
RUN rm -rf data/**/stats && (cd data && for d in */; do mkdir $d"stats"; done)


# Build with 
# docker build -t mmatusovic/python:run -f Dockerfile.run .
